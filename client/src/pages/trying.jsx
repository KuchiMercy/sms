import React, { useState } from 'react'

const Trying = () => {
const [verifyNumber, setVerifyNumber] = useState({
    
})
  
    const joined = Object.values(verifyNumber).join("")
  return (
    <div>trying</div>
  )
}

export default Trying